# Stripe payment channel configuration
# Copy needed keys into your runtime env file (for example .env.local)

# Required for creating Stripe Checkout sessions
STRIPE_SECRET_KEY=

# Optional but strongly recommended for webhook signature verification
STRIPE_WEBHOOK_SECRET=
