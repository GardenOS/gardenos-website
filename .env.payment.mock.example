# Mock payment channel configuration
# Default behavior allows mock fallback unless set to false

# true or false
# Set to false in production when all real providers are ready
PAYMENT_ALLOW_MOCK_FALLBACK=true
